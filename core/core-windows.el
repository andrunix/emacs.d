;; The only place you will catch me using Windows is at work.
;; And of course I can't access these repos directly so
;; use the proxies I convinced them to setup!

(setq package-archives
      '(("melpa"        . "http://nexus.bcbst.com:8080/repository/melpa-proxy/")
        ("org"          . "http://nexus.bcbst.com:8080/repository/elpa-proxy/")))

(provide 'core-windows)
